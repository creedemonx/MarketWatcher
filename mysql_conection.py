# import mysql.connector
# conection = mysql.conection.connect(
#     user = "root" , 
#     password = "admin", 
#     host = "localhost", 
#     database = "market_watcher", 
#     port = 3306) 
# cursor = conection.cursor()
# cursor.execute("SELECT * FROM users")
# users = cursor.fetchall()
# print(users)

# I will create a class called MySQLConection that will have a method called get_users that will return all the users from the users table in the market_watcher database.

# This will be implemented later in the stock_comparison.py file. 
